// ── Transition Manager [P3] ──
// Orchestrates screen transitions: Hub -> Hotspot -> Game -> Results -> Hub.

export function transitionTo(fromScreen, toScreen, type = 'fade') {
  // TODO [P3]: Animate transition between screens
}

export function showWalkSequence(fromHotspot, toHotspot) {
  // TODO [P3]: Show walking/running animation between hotspots
}

export function showDeliveryTransition() {
  // TODO [P3]: Show trash delivery animation at base
}
