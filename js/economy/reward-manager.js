// ── Reward Manager [P2] ──
// Calculates round rewards, streak bonuses, and checks achievements.

export function calculateRoundRewards(results) {
  // TODO [P2]: Calculate coins, XP, bonus rewards based on performance
  return { coins: 0, xp: 0, bonuses: [] };
}

export function checkAchievements(playerState) {
  // TODO [P2]: Check and unlock achievements
  return [];
}

export function getStreakBonus(consecutiveDays) {
  // TODO [P2]: Calculate daily streak bonus
  return 0;
}
