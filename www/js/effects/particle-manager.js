// ── Particle Manager [P3] ──
// Particle system for hit effects, combos, level-ups.

export function emitParticles(x, y, type = 'hit') {
  // TODO [P3]: Spawn particle burst at position
  // Types: 'hit' (correct sort), 'error' (wrong sort), 'combo' (high combo), 'levelup'
}

export function clearParticles() {
  // TODO [P3]: Remove all active particles
}
