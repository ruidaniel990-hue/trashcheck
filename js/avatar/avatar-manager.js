// ── Avatar Manager [P2] ──
// Manages avatar state, equipment slots, and gameplay bonuses from gear.

export function getEquippedItems() {
  // TODO [P2]: Return currently equipped items
  return {};
}

export function equipItem(itemId, slot) {
  // TODO [P2]: Equip item to slot
}

export function getEquipmentBonuses() {
  // TODO [P2]: Calculate total bonuses from equipped items
  return { comboBonus: 0, timeBonus: 0, coinMultiplier: 1 };
}
