// ── Transition Effects [P3] ──
// Visual effects: fade, slide, zoom animations between screens.

export function fadeOut(element, duration = 300) {
  // TODO [P3]: Fade out animation
  return new Promise(resolve => setTimeout(resolve, duration));
}

export function fadeIn(element, duration = 300) {
  // TODO [P3]: Fade in animation
  return new Promise(resolve => setTimeout(resolve, duration));
}

export function slideLeft(element, duration = 400) {
  // TODO [P3]: Slide left animation
  return new Promise(resolve => setTimeout(resolve, duration));
}

export function slideRight(element, duration = 400) {
  // TODO [P3]: Slide right animation
  return new Promise(resolve => setTimeout(resolve, duration));
}
